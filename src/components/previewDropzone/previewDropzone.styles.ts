import { createStyles } from "@mantine/core";

const usePreviewDropzoneStyles = createStyles((theme) => ({
	errorDropzone: {
		borderColor: theme.colors.red[8],
	},
}));

export default usePreviewDropzoneStyles;
